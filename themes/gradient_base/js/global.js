/**
 * @file
 * Global utilities.
 *
 */
(function ($, Drupal) {

    'use strict';
  
    Drupal.behaviors.gradient_base = {
      attach: function (context, settings) {
  
      }
    };
  
  })(jQuery, Drupal);
  